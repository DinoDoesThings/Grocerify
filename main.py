# Description: This is the main file that runs the login system. It creates an instance of the LoginSystem class and runs the run method.
from login_system import LoginSystem

if __name__ == "__main__":
    login = LoginSystem()
    login.run()
